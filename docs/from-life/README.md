# from-life — Jigzle material salvaged from the `life` repo

Jigzle planning artifacts that used to live in the personal `life` knowledge repo, moved
here (2026-07) so **all Jigzle content lives in `jigzle`**, its canonical home.

- `JZ-001-orders-pipeline.md` — the Orders-pipeline window spec (consolidate
  New/Pending/Fulfill/Outbound into one Orders window with pipeline tabs). Note: the ops app
  already ships Sales/Fulfill/Outbound/Receiving modules (`docs/003`–`006`), so review this
  against what's built — parts may already be done or superseded.
- `JIGZLE Import Cost Calculator.xlsx` — the original import-cost calculator spreadsheet; the
  prototype behind the live `apps/calculator` app. Kept as history/reference.
- `logo_jigzle.png` — Jigzle brand logo.
- `BACKLOG-jigzle.md` — the Jigzle backlog extracted from `life/BACKLOG.md`.
