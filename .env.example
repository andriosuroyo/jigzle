NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY
# NOTE: ALLOWED_USER_EMAIL was retired in migration 0017. The allow-list now lives in the DB
# table public.allowed_users, read by is_allowed_user(). Both apps' gates call that function via
# RPC, so there is no login env var — add/remove a user with a row in allowed_users.
