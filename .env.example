NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY
# Allowed logins — comma-separated (no spaces needed; trimmed + lowercased by the app).
# This is the app's route/UX gate. The authoritative data gate is the DB's is_allowed_user()
# RLS function (supabase/migrations/0016_allowed_users.sql) — keep its list in sync with this.
ALLOWED_USER_EMAIL=andriosuroyo@gmail.com
